# Python dependencies for make-icons.py
# Install with: pip install -r requirements.txt

# Required for PNG image processing
pypng>=0.0.20

# Required for LZ4 compression (optional, only needed when using --compress LZ4)
lz4>=4.0.0

